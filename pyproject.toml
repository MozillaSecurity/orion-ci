[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "orion-ci"
version = "v0.0.10"
dependencies = [
    "orion-decision @ git+https://github.com/MozillaSecurity/orion/@v0.0.10#subdirectory=services/orion-decision",
]
